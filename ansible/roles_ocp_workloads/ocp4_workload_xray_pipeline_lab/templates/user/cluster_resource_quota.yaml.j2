apiVersion: quota.openshift.io/v1
kind: ClusterResourceQuota
metadata:
  name: "{{ ocp4_workload_xray_pipeline_lab_user_quota_name }}"
spec:
  selector:
    annotations:
      openshift.io/requester: "{{ t_user }}"
    labels: null
  quota: {{ ocp4_workload_xray_pipeline_lab_user_quota | to_json }}
